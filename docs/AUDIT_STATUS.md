# 当前核查记录

记录日期：2026-09-18（本机北京时间，本次工作跨过午夜）。

## 来源

完整参考包：
`/home/lpq/a2g_mambakt/independent_a2g_ffn_gate_20260911_v33_frozen.zip`

服务器与本地压缩包 SHA256 相同：
`67750770da2a70df363d77b77635f01707e2586e685486aa4d4786f7d2f0509a`

冻结清单 148 项全部验证通过。另取回 v43 的
`input_memory_candidate.py`、`modal_residual_candidate.py`，
各自按 v43 manifest 核查。**v43 整个包并未完整同步**，
不能声称全部服务器实验材料已审计。

来源映射：`provenance/source_map.json`。
另取回七份 v43 设计、协议、历史闭合与审计材料，逐份与既有服务器 manifest
核对，结果见 `provenance/review_20260918/receipt.json`。

## 历史结果

读取到的服务器记录：

| 版本 | Assist2017 验证 AUC | 状态 |
| --- | --- | --- |
| 原单主干 | 0.7857959831005806 | 历史 JSON 记录，未在本机重训 |
| v33 | 0.8014311204701983 | 历史 JSON 记录，未在本机重训 |
| v43 | 未确认 | 已取回模型候选，不能推定训练完成 |

历史结果不是本地新实验，也不是本次独立重算预测得到的数值。

## 本地工程验证

- 单一主前向，常规导入不再依赖 `work.*` 或服务器绝对路径。
- v33/v43 同环境状态字典、预测和梯度等价。
- 激活附加模块后的对照、逐项消融、当前/未来回答隔离。
- 检查点重载、学生批间隔离、ID 和 fold 协议、训练先验隔离。
- RTX 3060 Laptop 6GB 已执行 v43 width256 / length200 / batch64 合成训练一步：
  5114869 参数，峰值 allocated 4827106304 字节，梯度有限。
- 合成一步的可运行性不代表完整训练已完成，也不代表 AUC 门槛通过。
- 生产初始化另行核查：114 个状态张量与服务器原版的随机初始化及训练折先验一致。
- 低显存配置通过 35 项测试；CPU 三次 Adam 更新及 CUDA 梯度对照逐位一致。
- 真实数据低显存 smoke 与原 smoke 的模型、预测文件 SHA256 完全相同。
- 低显存配置保持 batch64 和完整模型，峰值 allocated 为 2670045184 字节。
- 五个完整代码目录分别保存于 `versions/v33`、`versions/v43`、
  `versions/v43_lowmem`、`versions/v43_lowmem_r2`、`versions/v43_lowmem_r3`。
  所有已交付包保持冻结；`v43_lowmem_r2` 为 211 文件、`r3` 为 212 文件的
  独立完整副本，均通过各自 VERSION 哈希核查。
- 原前三个冻结包各执行 49 个测试条目，其中 8 个因测试类导入而重复；
  实际为 41 项不同测试，均通过。根目录已修正重复发现，冻结包不回写。
- v43 与 v43_lowmem 独立入口 CPU smoke 的模型和预测文件 SHA256 一致；
  均只训练 1 个 batch，不作性能证据。
- 根目录最新 CPU 报告 `runs/tests_versions_r3_cpu_fixed_20260918.json`：
  64 项中 62 项通过、2 项 CUDA 测试跳过。`r3` 自带入口 58 项中 56 项通过、
  2 项跳过。新增搬移导入、篡改检测、配置防串用、终审绑定和复评入口检查。
- 先前 `runs/tests_versions_r2_20260918.json` 的 62 项全部通过，但包含两项
  小规模 CUDA 测试，不能称为 CPU-only；当时空字符串环境变量未屏蔽 CUDA。
  后续用 `CUDA_VISIBLE_DEVICES=-1` 验证 CUDA 不可用，未再并发运行 GPU 测试。
- `r2` 和更早包的测试报告无法序列化跳过项；`r3` 在新目录中修复，旧包不回写。
  根目录直接执行测试工具的一处导入问题也已修复；冻结 `r3` 使用 `run.py tests`。
- `r2/r3` CPU 真实数据 smoke 和独立 CSV 审计通过，模型及预测 SHA256 与
  原 v43 和低显存版本逐字节一致。仅 1 个训练 batch，不作性能证据。
- 三版 CPU smoke 及低显存版 CUDA 文件日志 smoke 均通过独立 CSV/指标审计；
  CUDA 文件日志子进程退出码为 0，模型与预测哈希保持此前 smoke 的结果。
- 三个版本直接 batch256 均在前向阶段 OOM；低显存版不设额外比例限制时
  峰值 allocated 5700026368 字节。batch128 仅合成一步通过，
  allocated 5261673472 字节，不代表长训稳定或 AUC 改进。

## 中断记录

`v43_full_fold0_seed42_20260918` 完成四轮，第 4 轮 AUC 为
0.7591054970354288，随后发生 `OSError: [Errno 22] Invalid argument`。
原 traceback 缺失，写盘顺序指向控制台输出环节，但根因没有完全确定。

文件日志运行 `v43_full_logged_fold0_seed42_20260918` 在显存与系统内存
压力下长时间未完成首轮，核对进程身份后人工停止。两次都不是完整实验，
没有用已训练权重继续初始化，没有把它们计为性能失败。

实际正式运行的事前记录在
`runs/v43_lowmem_independent_decision_20260918_0325.json`；
更早的执行方案保留在 `runs/v43_lowmem_execution_decision_20260918.json`。
当前正式运行使用冻结版本 `versions/v43_lowmem`、batch64、fold0、seed42，
仍在进行中；未使用 batch256，也未加载训练检查点。最终结果以 `result.json`、
进程退出码、独立 CSV 审计和重载复评为准。
运行目录：`versions/v43_lowmem/runs/full_fold0_seed42_20260918_0325`。
开始时间为 2026-09-18 03:25:11 +0800。正式运行的初始化报告已验证
114 个状态张量与服务器原类及训练折先验一致。没有自动重试。
2026-09-18 04:43 +0800 的观察快照：已完成 34 轮，第 32 轮最佳 AUC
为 0.8011643751561428；进程仍在运行、无 result.json 和 incomplete.json。
该数值是中途验证日志，不是完整终审结果。交付与待办记录见
`runs/version_delivery_20260918_0443.json`。

## 尚未证明

- Assist2017 完整独立重训后 AUC 严格 > 0.8174。
- v43 相对于 v33 的正向 AUC 收益。
- 各模块独立重训消融均为正向提升。
- 同协议全部基线比较，以及同一组五个数据集的“5/5”。

## V44 本地迭代

在独立完整目录 `versions/v44_aligned_history_local` 保存了历史统计窗口候选，
216 个文件经 VERSION 校验。基础统计仅增加最近已观察的 t-1 事件；
旧模型和当前 v43 训练保持原行为。候选不是服务器等价迁移或性能改善结论。

- 根目录 CPU 回归72项中70项通过、2项 CUDA 测试跳过；冻结包66项中64项通过、
  2项跳过。初始化、参数和随机流与 v43 一致，关闭新开关后三步训练逐位一致。
- 真实数据 smoke、完整 CSV 绑定审计、服务器原类的训练折先验初始化核查通过。
- 训练折只读统计显示472860/606414个计分位置的最近概念相同，
  42009个位置原窗口漏掉该概念唯一一次出现。报告：
  `runs/v44_training_history_profile_20260918.json`。
- 条件执行记录：`runs/v44_aligned_history_prospective_20260918.json`。
  在旧运行正常结束及终审前，不启动本候选的正式训练。
- 另加根目录无题目输入测试，确认可训练性及题目尝试模块恒零的适用性限制；
  这些后续分析测试不回写已冻结候选。

同五数据集全部要求及资源、基线可比性限制见 `docs/GOAL_EVIDENCE.md`。
